name: Terraform CI/CD v2

on:
  workflow_dispatch:
  
  push:
    branches:
      - dev
    paths:
      - terraform/**
  pull_request:
    branches:
      - dev

env:
  TF_VERSION: "1.5.7"
  TF_ROOT: ./terraform
  TF_LOG: INFO

jobs:
  terraform:
   name: "Terraform Infrastructure Change Management"
   runs-on: ubuntu-latest
   environment: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}
   defaults:
     run:
       shell: bash
       working-directory: ${{ env.TF_ROOT }}
 
   steps:
     - name: Checkout the repository to the runner
       uses: actions/checkout@v2
 
     - name: Setup Terraform with specified version on the runner
       uses: hashicorp/setup-terraform@v3
       with:
         terraform_version: ${{ env.TF_VERSION }}

     - name: log work dir
       id: logs
       run: echo "$AWS_DEFAULT_REGION"

     - name: Configure AWS credentials
       uses: aws-actions/configure-aws-credentials@v4
       with:
          aws-access-key-id: ${ { secrets.AWS_ACCESS_KEY_ID } }
          aws-secret-access-key: ${ { secrets.AWS_SECRET_ACCESS_KEY } }
          aws-region: "us-east-1"
    
     
    
     - name: Terraform init
       id: init
       run: terraform init -backend-config="bucket=$TF_BUCKET_STATE"
 
     - name: Terraform format
       id: fmt
       run: terraform fmt -check
    
     - name: Terraform validate
       id: validate
       run: terraform validate

  
  # terraform-fmt:
  #   name: Terraform Format Check
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ${{ env.TF_ROOT }}
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Initialize Terraform
  #       run: terraform init -input=false

  #     - name: Check Terraform format
  #       run: terraform fmt -check

  # terraform-validate:
  #   name: Terraform Validate
  #   runs-on: ubuntu-latest
  #   needs: setup
  #   defaults:
  #     run:
  #       working-directory: ${{ env.TF_ROOT }}
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Terraform Init
  #       run: terraform init -input=false

  #     - name: Terraform Validate
  #       run: terraform validate

  # terraform-plan:
  #   name: Terraform Plan
  #   runs-on: ubuntu-latest
  #   needs: terraform-validate
  #   defaults:
  #     run:
  #       working-directory: ${{ env.TF_ROOT }}
  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Set up Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Terraform Init
  #       run: terraform init -input=false

  #     - name: Terraform Plan
  #       run: terraform plan -var-file=environment/datalake/dev.tfvars

  # terraform-apply:
  #   name: Terraform Apply (non-main only)
  #   if: github.ref != 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   needs: terraform-plan
  #   defaults:
  #     run:
  #       working-directory: ${{ env.TF_ROOT }}
  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Set up Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Terraform Init
  #       run: terraform init -input=false

  #     - name: Terraform Apply
  #       env:
  #         ANSIBLE_VAULT_PASS: ${{ secrets.ANSIBLE_VAULT_PASS }}
  #       run: |
  #         echo "$ANSIBLE_VAULT_PASS"
  #         terraform apply -var-file=environment/datalake/dev.tfvars -auto-approve
